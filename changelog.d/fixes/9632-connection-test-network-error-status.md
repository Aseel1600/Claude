- **api:** keep the connection status when a test never reaches the upstream — a connection test that fails with a network-level error (ENOTFOUND, "fetch failed", timeout) now preserves the existing testStatus instead of recording it as "error", preventing transient network blips from marking connections as broken in the dashboard. Includes a regression test ([#9632](https://github.com/diegosouzapw/OmniRoute/issues/9632))
