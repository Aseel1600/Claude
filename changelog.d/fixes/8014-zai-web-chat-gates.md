- **fix(providers):** `zai-web` (chat.z.ai) chat calls now reach upstream — the executor sends the `X-FE-Version` header that clears the in-SSE `426 client version outdated` gate, signs each request (`X-Signature` double-HMAC + browser fingerprint + `signature_timestamp`), and forwards a per-message Aliyun captcha token from `providerSpecificData.captchaVerifyParam` (string or array; spent one per message from a TTL-bounded pool, also accepted via the `x-omniroute-zai-captcha` header). In-stream error envelopes now surface as real HTTP errors instead of an empty completion. Static catalog refreshed to the 14 live model ids (retired `glm-4.6`/`glm-4.5`/`glm-4.5v` removed, default is now `glm-4.7`) and model discovery de-flagged as live-verified ([#8014](https://github.com/diegosouzapw/OmniRoute/issues/8014))
