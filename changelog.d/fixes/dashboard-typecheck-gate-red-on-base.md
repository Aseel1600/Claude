- Fix two TypeScript regressions under `src/app/(dashboard)/` that failed the `check:dashboard-typecheck` quality gate on `release/v3.8.50` itself, blocking CI for every open PR against that branch. `getI18nOrFallback()` in the combos page gained a required 4th `values` parameter, breaking all 187 existing 3-argument call sites (TS2554); it is now optional. `HarImportButton` narrowed its `M365HarImportResult` union with `!result.ok`, which does not narrow boolean-literal discriminants under the repo's `strict: false` config (TS2339); it now compares `result.ok === false`.
