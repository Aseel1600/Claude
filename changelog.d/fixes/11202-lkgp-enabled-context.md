- **fix(routing):** Settings → Routing "last known good provider" toggle now takes effect — `lkgpEnabled` was persisted and the `lkgp` strategy guarded on it, but the setting was never forwarded into the `RoutingContext` built in `resolveAutoStrategyOrder()`, so the off-switch was unreachable ([#11181](https://github.com/diegosouzapw/OmniRoute/issues/11181) / [#11202](https://github.com/diegosouzapw/OmniRoute/pull/11202))
