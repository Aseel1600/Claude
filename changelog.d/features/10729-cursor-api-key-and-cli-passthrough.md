- **feat(providers):** Cursor accepts a pasted `crsr_…` user API key next to OAuth/IDE-session connections (the key is exchanged for a 1h session token and cached), and a new `/api/cursor-cli/*` passthrough lets the Cursor CLI run through OmniRoute (`CURSOR_API_ENDPOINT=http://<omniroute>/api/cursor-cli`, `CURSOR_API_KEY=<OmniRoute key>`) with every RPC attributed and logged. (#10729)
