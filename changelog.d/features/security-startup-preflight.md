- **feat(cli):** boot-time security preflight for `omniroute serve` — audits the resolved configuration before anything listens and reports the localhost-tuned defaults that are actually in effect for this boot (network-exposed anonymous `/v1` API, plaintext credential store, `.env.example` placeholder password, non-Secure session cookies). Advisory by default; `OMNIROUTE_SECURITY_PREFLIGHT=strict` refuses to start on a CRITICAL finding for unattended deploys, `off` silences it. See `docs/security/STARTUP_PREFLIGHT.md`.
