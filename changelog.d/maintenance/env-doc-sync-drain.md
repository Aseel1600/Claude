- **chore(docs):** drain the `check:env-doc-sync` backlog — document `KIMI_WEB_BASE_URL` / `KIMI_WEB_CHAT_URL` (kimi-web executor endpoint overrides), `OMNIROUTE_OIDC_DISABLE_PASSWORD_LOGIN` and its legacy `OIDC_DISABLE_PASSWORD_LOGIN` alias (password login disabled while OIDC is active), and `ELECTRON_SMOKE_COLD_RESTART` (missed when the smoke harness gained it) in `.env.example` + `docs/reference/ENVIRONMENT.md`; allowlist the test-only build seam `OMNIROUTE_STANDALONE_DIR`. The env/docs contract gate is now fully green in all three directions.
